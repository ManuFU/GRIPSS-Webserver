apiVersion: v1
kind: Service
metadata:
  name: gripss-webserver-service
  namespace: default
spec:
  selector:
    app: gripss-webserver
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

