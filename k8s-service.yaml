apiVersion: v1
kind: Service
metadata:
  name: gripss-webserver-service
  namespace: gripss-dev
spec:
  selector:
    app: gripss-webserver
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
